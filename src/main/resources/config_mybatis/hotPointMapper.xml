<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stream.mokulive.storage.mapper.HotPointMapper">


    <insert id="addHotPoint" parameterType="stream.mokulive.storage.vo.HotPoint">
        insert into storage_hot_point values (
            #{id},
            #{userId},
            #{buildingId},
            #{xstart},
            #{ystart},
            #{xend},
            #{yend},
            #{hotPointName},
            #{comment},
            #{status}
        )
    </insert>

    <update id="updateHotPoint" parameterType="stream.mokulive.storage.vo.HotPoint">
        update storage_hot_point set
            userid=#{userId},
            buildingid=#{buildingId},
            xstart=#{xstart},
            ystart=#{ystart},
            xend=#{xend},
            yend=#{yend},
            hotpointname=#{hotPointName},
            comment=#{comment},
            status=#{status}
            WHERE
            id=#{id}
    </update>

    <update id="deleteHotPoint" parameterType="hashmap">
        update storage_hot_point
          SET status='0'
          WHERE
          id=#{id}
    </update>

    <select id="findHotPointById" parameterType="hashmap" resultType="stream.mokulive.storage.vo.HotPoint">
        select * from storage_hot_point
          WHERE
          id=#{hotPointId}
    </select>

    <select id="findAllHotPointList" parameterType="hashmap" resultType="stream.mokulive.storage.vo.HotPoint">
        select * from storage_hot_point
          WHERE
          userid=#{userId}
          and status='1'
    </select>


</mapper>